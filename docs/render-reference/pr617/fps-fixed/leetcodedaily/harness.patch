--- a/src/ui.rs
+++ b/src/ui.rs
@@ -315 +315,6 @@
-    launcher_with_size(APP_WIDTH, APP_HEIGHT).run(App);
+    let launcher = launcher_with_size(APP_WIDTH, APP_HEIGHT);
+    if std::env::var_os("PR617_PERF_OUTPUT").is_some() {
+        launcher.with_frame_pacing_mode(cranpose::FramePacingMode::Vsync).with_test_driver(|robot| perf_driver::run(&robot).expect("LeetCodeDaily measurement")).run(App);
+    } else {
+        launcher.run(App);
+    }
@@ -6982,0 +6988,4 @@
+
+#[cfg(not(target_arch = "wasm32"))]
+#[path = "perf_driver.rs"]
+mod perf_driver;
--- a/src/draft.rs
+++ b/src/draft.rs
@@ -920 +920 @@
-    std::env::var_os("HOME")
+    std::env::var_os("PR617_DATA_HOME").or_else(|| std::env::var_os("HOME"))
@@ -936 +936 @@
-    std::env::var_os("HOME")
+    std::env::var_os("PR617_DATA_HOME").or_else(|| std::env::var_os("HOME"))
@@ -1133 +1133 @@
-    std::env::var_os("HOME")
+    std::env::var_os("PR617_DATA_HOME").or_else(|| std::env::var_os("HOME"))
